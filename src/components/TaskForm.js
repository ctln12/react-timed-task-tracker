import React from 'react';

const Taskform = () => {
  return (
    <form className='TaskForm'>
      <input
        type='text'
        placeholder='Add a new task'
      />
    </form>
  );
}

export default Taskform;
